---
title: Windows SDK Setup
sidebar_position: 2
description: Windows SDK integration guide.
---

import {useEffect} from 'react';
import {useHistory} from '@docusaurus/router';

export function AutoRedirect() {
  const history = useHistory();
  useEffect(() => { history.replace('/reference/windows-sdk-setup'); }, []);
  return null;
}

<AutoRedirect />

This page has moved. You will be redirected to the [Windows SDK — Setup & Integration](/reference/windows-sdk-setup) guide.
